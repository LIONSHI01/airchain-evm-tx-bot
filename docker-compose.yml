version: "3"

services:
  node-app:
    build: .
    volumes:
      - .:/usr/src/app
    ports:
      - "${PORT:-31111}:31111"
    environment:
      NODE_ENV: development
        - .env
    command: /bin/sh -c "yarn && yarn start"
    restart: always
